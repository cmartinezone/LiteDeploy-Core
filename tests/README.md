# Tests

| Path | Role |
| --- | --- |
| [WinPEEnv](WinPEEnv/) | Generated WinPE + share layout. Sync from `components/` after changes; start on Windows with `Start-WinPETestEnv.ps1`. |

Pester / headless `-Probe` coverage is not here yet. `experiments/` is not this harness and does not promote.
